(ns typos.core
  "Utilities for writing command-line applications")

(defn send
  "Send a message to the browser."
  [b]
  ;; send length
  ;; send bytes
  )

(defn read
  "Returns the next message synchronously."
  ;; read length
  ;; read bytes
  )
